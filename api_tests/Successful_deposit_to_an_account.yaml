# Test generated by RoostGPT for test qlue-single-feature-integration using AI Type Open AI and AI Model gpt-4


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "Successful_deposit_to_an_account.csv"
    fields:
      - "customerId"
      - "sourceAccountId"
      - "amount"
      - "currency"
      - "frontImage"
      - "backImage"
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful deposit to an account"
    # This scenario tests a successful deposit operation
    flow:
      - post:
          url: "/deposit"
          json:
            customerId: "{{ customerId }}"
            sourceAccountId: "{{ sourceAccountId }}"
            amount: "{{ amount }}"
            currency: "{{ currency }}"
            image:
              frontImage: "{{ frontImage }}"
              backImage: "{{ backImage }}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
            botId: "botId"
            accountId: "accountId"
          expect:
            - statusCode: 200
            # Assert that the response includes a transactionId property
            - hasProperty: "transactionId"
          think: 1
